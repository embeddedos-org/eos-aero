# Development

## Contribution source of truth

No root `CONTRIBUTING` file was found on `master` during this publication.

Before proposing a change, also review the [README](https://github.com/embeddedos-org/eos-aero/blob/master/README.md). Keep changes scoped, add tests appropriate to the affected behavior, and follow the repository's current automation and review requirements.

## Build and dependency inputs found

`AeroSwift/software/web_app/package.json`, `AeroSwift/software/web_app/pnpm-lock.yaml`.

## Tests found in the default-branch tree

`AeroSwift-Personal/docs/specs/.gitkeep`, `AeroSwift-Transit/docs/specs/.gitkeep`, `AeroSwift/docs/specs/app_design_spec.md`.

## Documented test commands

No test command was quoted here because no supported test command was found in a shell code block in the root README or contributing guide. Consult those files and the test paths below before running project-specific tooling.

## Verification baseline

This inventory comes from `master` at [`14bdc06a8326`](https://github.com/embeddedos-org/eos-aero/commit/14bdc06a8326d2a4238ee362c1d610cbfdfad34a) and found 3 test-related paths among 161 files. Re-check the source tree when that commit is no longer current.
